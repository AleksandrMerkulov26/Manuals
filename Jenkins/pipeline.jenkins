#!groovy
// Check test.localhost properties
properties([disableConcurrentBuilds()])

pipeline {
    agent {
        label 'main'
    }
    options {
        buildDiscarder(logRotator(numToKeepStr: '10', artifacNumToKeepStr: '10'))
        timestamps()
    }
    stages {
        stage("First step") {
            steps {
                sh 'ssh test.localhost \'hostname\''
            }
        }
    }
}